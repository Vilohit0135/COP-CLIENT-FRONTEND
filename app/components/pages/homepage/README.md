This folder contains the homepage entry component and related homepage-specific components.

- `index.tsx` — re-exports the main `Homepage` component.
- `Homepage.tsx` — main page component (kept in parent folder for now).

Move other homepage-specific components here as needed (Hero, Stats, etc.).
